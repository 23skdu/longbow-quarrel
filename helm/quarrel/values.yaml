replicaCount: 1

image:
  repository: ghcr.io/23skdu/longbow-quarrel
  pullPolicy: IfNotPresent
  tag: "latest"

model:
  name: "smollm2:135m-instruct"
  # Path to model file if not using ollama naming
  # path: "/models/model.gguf"

service:
  type: ClusterIP
  port: 8080

resources:
  limits:
    cpu: 2
    memory: 4Gi
  requests:
    cpu: 1
    memory: 2Gi
